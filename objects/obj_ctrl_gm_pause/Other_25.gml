/// @description выполнение действий
/*
switch (action) {
	case "goToPause":
		//создаем копию нашей комнаты, чтобы сохранить состояние экземпляров
		obj_ctrl_mv.ctrl_mv_rmDublicate = room_duplicate(room);
		room_goto(rm_menu_pause);
		obj_ctrl_mv.action = "addRm";
	break;
}
