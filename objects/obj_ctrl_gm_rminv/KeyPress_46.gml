/// @description удаление по одному предмету или сразу пачку

if (!obj_ctrl_dlg.ctrl_dlg_exist) {	// если нет диалога
	action = "invCheckDelCell";	// проверка на то, надо ли вызывать диалог перед удалением всех предметов в ячейке
}
